- content_for :title do
  Failed Downloads

.p-4
  = render BreadcrumbsComponent.new(crumbs: ["Downloads", "Failed Downloads"])

.p-4  
  = render DownloadsComponent.new(downloads: @downloads, status_views: [:queued, :started, :failed], actions: [:remove, :queue])
