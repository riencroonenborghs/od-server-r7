- content_for :title do
  Finished Downloads

.downloads.downloads--finished
  = breadcrumb("Downloads", "Finished Downloads")
  = render DownloadsComponent.new(downloads: @downloads, status_views: [:queued, :started, :finished], actions: [:remove, :queue])
  -# = render "table", table: { queued: true, started: true, finished: true }, action: { remove: true, queue: true }
